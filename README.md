# Traffic-Light
Проект светофора от 23.02.2026

# Умный перекрёсток с адаптивными фазами и диспетчеризацией (ПР-231)

**Команда №1**

## 📖 О проекте

Проект представляет собой прототип контроллера «Умного перекрёстка» на базе микроконтроллера ESP32. Система предназначена для управления дорожным трафиком по двум осям (Север-Юг и Восток-Запад) с использованием конечного автомата (FSM). 

Ключевая особенность — адаптивность: контроллер способен продлевать зеленый сигнал при наличии потока машин и обеспечивать безопасную фазу для пешеходов. Оператор системы получает полный контроль через HMI-интерфейс (OLED-дисплей + энкодер) с возможностью настройки таймингов и просмотра диагностики.

### Основные функции:
*   Управление светофорами (8 LED: авто + пешеходы).
*   Адаптивное изменение длительности фаз.
*   Режимы: Стандартный, Ночной (мигающий желтый), Аварийный.
*   Интерфейс оператора (вывод статуса и настройка параметров).

## 👥 Команда и роли

*   **[ФИО Лидера]** — Team Lead / Архитектор (Настройка репозитория, FSM ядро).
*   **[ФИО Участника 1]** — Embedded Developer (Драйверы периферии, OLED, Энкодер).
*   **[ФИО Участника 2]** — QA / Developer (Логика адаптации, тесты, документация).

## 🎯 MVP (Минимально жизнеспособный продукт)

В рамках текущей итерации (Must Have) будет реализовано:
1.  **Базовая логика светофора (FSM):** Циклическое переключение фаз (Зеленый -> Желтый -> Красный) без конфликтов сигналов.
2.  **Пешеходная фаза:** Реализация безопасного интервала "Всем красный" для перехода пешеходов по запросу.
3.  **Интерфейс оператора:** Отображение текущей фазы и обратного отсчета таймера на OLED дисплее.
4.  **Управление:** Имитация датчиков потока и кнопок пешеходов для переключения состояний.

## 📋 Управление задачами

Мы работаем по методологии Agile/Scrum. Список задач, бэклог спринта и статус выполнения доступны на нашей доске:

👉 **[Ссылка на GitHub Projects Board]** *(вставить ссылку сюда)*

## 🛠 Технический стек и оборудование (Wokwi)

Проект разрабатывается на **C++ (Arduino Framework)** для **ESP32**.

**Схема подключения (Pinout):**
*   **MCU:** ESP32 Dev Kit v1.
*   **Display:** SSD1306 OLED (I2C: SDA=GPIO21, SCL=GPIO22).
*   **RTC:** DS1307 (I2C: 0x68).
*   **Controls:**
    *   Encoder KY-040 (CLK, DT, SW).
    *   Buttons x4 (Пешеход, Поток НС, Поток ВЗ, Авария).
*   **Indication:**
    *   8 LED (Светофоры авто и пешеходные).
    *   Buzzer (Активный зуммер).

Подробное описание архитектуры и схемы находится в файле: [docs/architecture.md](./docs/architecture.md).

## 🚀 Как запустить симуляцию

Проект настроен для запуска в симуляторе **Wokwi**.

### Вариант 1: VS Code (Рекомендуемый)
1.  Установите расширение **Wokwi for VS Code**.
2.  Клонируйте репозиторий.
3.  Откройте папку проекта в VS Code.
4.  Нажмите `F1` и выберите `Wokwi: Start Simulator`.
5.  Симуляция соберет C++ код и запустит схему, описанную в `diagram.json`.

### Вариант 2: Wokwi Web
1.  Перейдите на сайт [wokwi.com](https://wokwi.com).
2.  Создайте новый проект для ESP32.
3.  Скопируйте содержимое `diagram.json` в вкладку Diagram.
4.  Скопируйте исходный код (`src/main.cpp` или `sketch.ino`) в вкладку Code.
5.  Запустите симуляцию.

## 📂 Структура репозитория

```text
.
├── .github/                # Настройки GitHub Actions (если есть)
├── docs/                   # Документация проекта
│   └── architecture.md     # Описание схемы и архитектуры
├── src/                    # Исходный код (C++)
│   └── main.cpp            # Точка входа (setup/loop)
├── test/                   # Автотесты (будущие спринты)
├── diagram.json            # Схема подключения для Wokwi
├── wokwi.toml              # Конфигурация Wokwi для VS Code
├── platformio.ini          # Конфигурация сборки (если используется PIO)
└── README.md               # Описание проекта

Проект выполняется в рамках лабораторной работы №2 (Спринт 2).

### Что еще нужно сделать (согласно заданию ЛР2):

1.  Создать файл `docs/architecture.md` и вставить туда краткое описание схемы (как указано в задании: "какая плата, компоненты, интерфейсы").
2.  Создать минимальный файл `src/main.cpp` (или `.ino`, если вы не используете PlatformIO), чтобы симуляция запускалась.
    *Пример для C++:*
    ```cpp
    #include <Arduino.h>

    void setup() {
      Serial.begin(115200);
      Serial.println("READY: Smart Crossroads System Started");
      // TODO: Init Hardware
    }

    void loop() {
      // TODO: Main FSM Loop
      delay(1000);
    }
    ```
3.  Создать `diagram.json` с минимальной конфигурацией (ESP32 + 1 светодиод), чтобы Wokwi не выдавал ошибку при запуске.
